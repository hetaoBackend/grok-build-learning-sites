# Contributing

感谢你愿意一起改进 grok-build Source Lab。这个项目首先是一份基于固定源码版本的技术解释，因此贡献的首要目标是：**让内容更容易理解，同时不丢失源码事实和边界条件。**

## 可以贡献什么

- 修正技术事实、crate 职责或源码链接；
- 改进中文解释、术语一致性和阅读顺序；
- 增加忠于源码状态机的交互场景；
- 改善键盘操作、移动端布局、reduced-motion 和对比度；
- 研究新的上游版本并记录与当前课程基线的差异。

## 内容证据要求

当前课程基线是 `xai-org/grok-build@dd04f397b1d02f2272b092555669dfba1f01bc85`。

提交技术纠错时，请尽量附上：

1. 固定 commit permalink；
2. crate、文件路径和关键符号；
3. 该结论属于源码事实、教学解释还是已知边界；
4. 如果涉及安全机制，同时说明保护范围和未覆盖范围。

不要仅引用上游默认分支，因为分支继续变化后，证据会漂移。

## 本地开发

需要 Node.js `>=22.13.0`。

```bash
npm ci
npm run dev
```

提交前请运行：

```bash
npm run lint
npm test
```

`npm test` 会执行生产构建，并验证六个专题路由、内容模型、固定源码证据和渲染结果。

## 修改课程内容

- 专题标题和学习顺序：`lib/content/modules.ts`
- 源码版本与链接：`lib/content/source.ts`
- 各专题证据和练习：`lib/content/*.ts`
- 各专题概念正文：`app/<module>/page.tsx`
- 共享教学结构：`components/ModuleLesson.tsx`

请复用已有的事实 / 解释 / 边界标签，不要把教学归纳写成源码原生抽象。

## Pull Request 建议

一个 PR 尽量只解决一个明确问题。描述中请写清：

- 改了什么；
- 为什么需要改；
- 如何验证；
- 是否改变技术结论或源码基线。

如果准备整体更新上游源码版本，请先创建 Issue，列出受影响专题和重新验证计划，避免不同基线的证据混在一起。
